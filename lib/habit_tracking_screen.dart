import 'package:flutter/material.dart';

class HabitTrackingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Habit Tracking')),
      body: Center(
        child: Text('Habit Tracking Calendar View'),
      ),
    );
  }
}
